<%= form_with(model: entry, local: true) do |form| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :term %>
    <%= form.text_field :term, id: :entry_term %>
  </div>

  <div class="field">
    <%= form.label :definition %>
    <%= form.text_area :definition, id: :entry_definition %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :entry_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
